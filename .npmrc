# NPM Registry Configuration
registry=https://registry.npmjs.org/

# @internal packages are local workspace packages - resolved via pnpm-workspace.yaml
# No registry needed for @internal scope

# Prefer offline cache when available
prefer-offline=true

# Engine strict mode
engine-strict=false

strict-ssl=false
